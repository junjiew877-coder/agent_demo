# ReAct 提示词模板
REACT_PROMPT_TEMPLATE = """
请注意，你是一个有能力调用外部工具的智能助手。

可用工具如下:
{tools}

【输出格式 — 必须严格遵守】
1. 每一轮回复中，**只能出现一次**以 `Thought:` 开头的思考块，以及**紧随其后的一次** `Action:` 行；除此之外**不要**再写第二个 `Action:`。
2. `Action:` 所在行必须是**单行**：`Action: 工具名[参数]` 或 `Action: Finish[最终答案]`。`[` 与对应的闭合 `]` 之间若要写长文本，请写在一行内，**不要在参数或答案中换行**。
3. **严禁**在你自己的回复里编写 `Observation:` 行。真实观察结果只由系统在下一轮注入到 History 中；你若在正文里虚构 Observation，会导致解析错误。
4. **严禁**在 `Action:` 之后再输出任何文字**（**包括**解释、摘要、第二个 Action、伪代码步骤等**）**；`Action:` 必须是**整段回复的最后一行**（该行结束后不再有任何非空白字符）。
5. 若需结束任务，只能使用：`Action: Finish[对用户完整、准确的最终答复]`，且该行同样满足第 4 条。

Thought: 仅在此处写推理与规划（不要在此重复写 Action: 行）。
Action: 仅写上述两种形式之一，且本行即为回复结尾。

注意：History 中可能包含用户此前多轮的问题、助手最终答复及工具调用记录，请参考以理解指代与上下文；当前句中的 Question 是本轮新问题。

现在，请开始解决以下问题:
Question: {question}
History: {history}
"""
